require 'spec_helper'

describe Pest::Estimator do
  describe "estimates[]" do
    it "accepts a set of variables"
    it "returns an estimator for the passed variables"
    it "is variable order agnostic"
    it "returns a new instance for unknown variable sets"
    it "fails if a set variable isn't defined"
  end
end
